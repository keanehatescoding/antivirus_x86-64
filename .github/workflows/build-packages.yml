name: Build distro packages

# Builds the .deb (Debian/Ubuntu), .pkg.tar.zst (Arch), and .rpm
# (Fedora) packages under packaging/ on every push/PR, so a packaging
# regression (a missing BuildRequires, a broken Makefile override, a
# stale pinned source) gets caught the same way build-matrix.yml
# catches a kernel-compat regression. Unlike that workflow, none of
# these three jobs touch av.ko at all: hyprav-dkms ships it as DKMS
# *source* in every one of these formats (see debian/rules,
# packaging/arch/PKGBUILD, and packaging/fedora/hyprav.spec's own
# comments) - actually compiling it happens later, on the end user's
# machine, against whatever kernel is really installed there, not at
# package-build time. This only verifies the PACKAGES build.

on:
  push:
  pull_request:
  # Lets .github/workflows/release.yml (tag-triggered) run these same
  # three jobs via `uses: ./.github/workflows/build-packages.yml`
  # instead of re-declaring deb/arch/rpm build steps a second time in
  # a second file - one definition, two callers. Every step below
  # behaves identically either way except the version-stamping steps
  # in build-deb/build-rpm, which are gated on `startsWith(github.ref,
  # 'refs/tags/')` and so are no-ops on a plain push/PR trigger.
  workflow_call:

# None of these jobs push, comment, or otherwise write anything back to
# the repository - read-only is enough, and persist-credentials: false
# below on every checkout means there's no token to abuse even if a
# build step somehow got compromised.
permissions:
  contents: read

jobs:
  build-deb:
    runs-on: ubuntu-latest
    # Debian/Ubuntu, not a generic ubuntu-latest apt install: these
    # exact build-dep package names (notably gir1.2-gtk-4.0) were
    # confirmed against a real Debian 13 (trixie) box (see
    # debian/control's own comment) - Ubuntu's repos aren't guaranteed
    # to carry the same names/versions.
    container: debian:trixie
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7
        with:
          persist-credentials: false

      - name: Install build dependencies
        run: |
          apt-get update -qq
          apt-get install -y -qq --no-install-recommends \
            build-essential dpkg-dev debhelper \
            pkg-config libnl-genl-3-dev libyara-dev libfuzzy-dev \
            python3-gi gir1.2-gtk-4.0

      # debian/changelog's only entry is hardcoded "hyprav (0.9.0) ...",
      # written once when packaging was added and never bumped since -
      # DEB_VERSION_UPSTREAM (debian/rules, via
      # /usr/share/dpkg/pkg-info.mk) reads straight off that top entry,
      # so every .deb this job ever built reported "0.9.0" regardless
      # of what was actually tagged. Harmless for the push/PR case
      # (nothing here claims to be a release), but wrong for
      # release.yml's tag-triggered call - stamp the real tag onto the
      # top entry's version field first, release builds only. Only the
      # first `(...)` on the file is touched (sed's `0,/re/` limits the
      # match to the top entry), which is exactly the version field per
      # debian's changelog format.
      - name: Stamp debian/changelog with the release tag's version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          set -euxo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          sed -i "0,/([^)]*)/{s/([^)]*)/(${VERSION})/}" debian/changelog
          dpkg-parsechangelog -SVersion

      - name: dpkg-buildpackage
        # Unsigned (-us -uc) - no GPG key available in CI, and these
        # aren't meant to be published as-is.
        run: dpkg-buildpackage -us -uc -b

      - name: Move built packages into the workspace
        # dpkg-buildpackage places output one directory ABOVE the
        # source tree it was invoked in, not inside it -
        # actions/upload-artifact rejects any path containing `..`
        # outright ("Relative pathing '.' and '..' is not allowed"),
        # so it can't just glob that parent directory directly.
        run: |
          mkdir -p dist
          mv ../*.deb ../*.buildinfo ../*.changes dist/

      - name: Upload .deb packages
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          name: deb-packages
          path: dist/*.deb
          if-no-files-found: error

  build-arch:
    runs-on: ubuntu-latest
    container: archlinux:base-devel
    steps:
      # git must exist BEFORE actions/checkout runs, not after - a
      # real CI run showed that without it, checkout silently falls
      # back to downloading a plain tarball via the GitHub REST API
      # ("To create a local Git repository instead, add Git 2.18 or
      # higher to the PATH"), which never creates a .git directory at
      # all. Every later git command (PKGBUILD's pkgver(), makepkg's
      # own git source clone) then fails with what looks like an
      # ownership/permission error but is really just "there is no
      # .git here" - archlinux:base-devel doesn't ship git installed.
      - name: Install git (before checkout needs it)
        run: pacman -Syu --noconfirm --quiet && pacman -S --noconfirm --needed --quiet git

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7
        with:
          persist-credentials: false
          # PKGBUILD's pkgver() runs `git describe --long --tags`,
          # which needs full tag history - the default shallow
          # (depth-1) checkout has none of it.
          fetch-depth: 0

      - name: Install bootstrap dependencies
        # Only what's needed to get makepkg itself running as a
        # non-root user - `makepkg -s` below auto-installs every real
        # package makedepend (libnl/yara/ssdeep/tlsh/pkgconf/
        # python-gobject/gtk4/...) straight from PKGBUILD itself, so
        # duplicating that list here would just be a second place it
        # could silently drift out of sync.
        run: pacman -S --noconfirm --needed --quiet sudo

      - name: makepkg (as a non-root builder user)
        run: |
          set -euxo pipefail
          # makepkg refuses to run as root, and root is this
          # container's only user - create an unprivileged builder,
          # matching every real Arch install, and let it sudo back to
          # root for the pacman calls `makepkg -s` needs to pull in
          # makedepends.
          useradd -m builder
          echo 'builder ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/builder

          # Copy the checkout to a location builder owns outright,
          # rather than chown -R'ing $GITHUB_WORKSPACE itself and
          # pointing HYPRAV_CI_SOURCE there in place - keeps the real
          # checkout's ownership untouched for every other step, and
          # matches what was actually build-tested locally before this
          # workflow was written.
          cp -r "$GITHUB_WORKSPACE" /home/builder/hyprav-ci-src
          chown -R builder:builder /home/builder/hyprav-ci-src

          # A real script file, not an inline su -c string - avoids a
          # quoting mess between this outer shell and the inner one,
          # and is what actually runs makepkg as the builder user.
          # Quoted heredoc (no outer interpolation) - HEAD_SHA below is
          # computed and expanded entirely by the INNER script instead,
          # as builder, against the copy builder already owns outright;
          # computing it here instead (as root, against $GITHUB_WORKSPACE)
          # hits the exact same dubious-ownership issue as build-rpm's
          # git commands, confirmed by a real CI run.
          cat > /tmp/build-arch-pkg.sh <<'SCRIPT'
          set -euxo pipefail
          cd /home/builder/hyprav-ci-src/packaging/arch
          # Pinned to the exact checked-out commit rather than left for
          # makepkg's git source to infer from HEAD on its own -
          # confirmed working either way against a real pull_request
          # checkout (whose HEAD is a detached commit, GitHub's own
          # synthetic PR-merge-preview commit, not a named branch), but
          # pinning removes any dependence on makepkg/git mirror-clone
          # HEAD-tracking behavior at all.
          HEAD_SHA="$(git rev-parse HEAD)"
          # HYPRAV_CI_SOURCE overrides PKGBUILD's git source, which
          # otherwise always fetches from GitHub regardless of the
          # local checkout, so this builds the exact commit under
          # test instead of whatever is currently on the default
          # branch (see PKGBUILD's own comment on this variable).
          export HYPRAV_CI_SOURCE="file:///home/builder/hyprav-ci-src#commit=$HEAD_SHA"
          # HYPRAV_SKIP_AV_KO_CHECK skips the av.ko compile-sanity
          # check: this container shares the CI runner's real host
          # kernel, but Arch's linux-headers package only matches
          # Arch's OWN kernel package, so it can never resolve here -
          # build-matrix.yml already covers av/ compiling correctly,
          # far more rigorously, elsewhere (see PKGBUILD's own comment
          # on this variable).
          export HYPRAV_SKIP_AV_KO_CHECK=1
          makepkg -s --noconfirm
          SCRIPT
          chmod +x /tmp/build-arch-pkg.sh
          su builder -c /tmp/build-arch-pkg.sh

          # Copy the built packages back into the real workspace (as
          # root, which can always write there) so the upload step
          # below finds them at the usual path.
          cp /home/builder/hyprav-ci-src/packaging/arch/*.pkg.tar.zst \
            packaging/arch/

      - name: Upload .pkg.tar.zst packages
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          name: arch-packages
          path: packaging/arch/*.pkg.tar.zst
          if-no-files-found: error

  build-rpm:
    runs-on: ubuntu-latest
    container: fedora:latest
    # No longer continue-on-error: this job used to have two stacked
    # bugs - BuildRequires: tlsh-devel never resolving on any shipping
    # Fedora release (fixed by vendoring TLSH as pure C, see
    # packaging/fedora/hyprav.spec's history) and the spec's
    # `%global gitcommit 32d9af8` winning over this job's own
    # `--define "gitcommit $GITCOMMIT"` (RPM's %global unconditionally
    # redefines, it doesn't just provide a default), which made the
    # %prep tarball lookup never match what `git archive` actually
    # produced below. The guarded `%if 0%{!?gitcommit:1}` form
    # (packaging/fedora/hyprav.spec) fixes the second one - confirmed
    # with a real rpmbuild run against this exact job's steps
    # (podman run fedora:latest, same dnf builddep + rpmbuild -bb
    # invocation), which produced Version: 0.9.0.129.g<the checked-out
    # commit>, not the stale 32d9af8 fallback.
    steps:
      # git must exist BEFORE actions/checkout runs, not after - a
      # real CI run showed that without it, checkout silently falls
      # back to downloading a plain tarball via the GitHub REST API
      # ("To create a local Git repository instead, add Git 2.18 or
      # higher to the PATH"), which never creates a .git directory at
      # all. The later `git rev-parse`/`git archive` calls below then
      # fail with "not a git repository" - not an ownership issue,
      # fedora:latest just doesn't ship git installed.
      - name: Install git (before checkout needs it)
        run: dnf install -y -q git

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Install rpmbuild tooling
        run: |
          dnf install -y -q rpm-build rpmdevtools dnf-plugins-core
          # actions/checkout's own dubious-ownership fix (its "Post Run"
          # step logs "Adding repository directory to the temporary git
          # global config as a safe directory") is scoped to a
          # temporary HOME override for its OWN internal git calls only
          # - it doesn't persist for any later step's git commands.
          # Confirmed by a real CI run: `git rev-parse` here failed
          # with "detected dubious ownership in repository at
          # '$GITHUB_WORKSPACE'" even though checkout itself succeeded.
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Build a source tarball from THIS checkout
        # hyprav.spec's Source0 names a specific historical commit's
        # tarball fetched from GitHub (%global gitcommit) - same
        # "designed for a real release, not a local CI checkout"
        # mismatch as packaging/arch/PKGBUILD's git source (see that
        # job's own comment). rpmbuild never actually fetches Source0
        # itself though (that URL is documentation for humans/tools
        # like spectool, not something rpmbuild executes) - it just
        # expects a local file under _sourcedir matching Source0's
        # basename, so regenerating that from the actual commit under
        # test is enough to make this validate the real tree once
        # unblocked, not a stale pinned commit.
        run: |
          set -euxo pipefail
          GITCOMMIT="$(git rev-parse --short=7 HEAD)"
          echo "GITCOMMIT=$GITCOMMIT" >> "$GITHUB_ENV"
          mkdir -p rpmbuild-sources
          git archive --prefix="antivirus-$GITCOMMIT/" HEAD \
            -o "rpmbuild-sources/antivirus-$GITCOMMIT.tar.gz"

      - name: Install BuildRequires (dnf builddep)
        run: |
          dnf builddep -y --define "gitcommit $GITCOMMIT" \
            packaging/fedora/hyprav.spec

      # Release builds only (see build-deb's changelog-stamping step
      # above for the same reasoning): hyprav.spec's Version defaults
      # to a fixed placeholder string (packaging/fedora/hyprav.spec's
      # own comment on the pkgversion guard) - override it with the
      # actual tag so the built .rpm's Version matches what
      # packaging/arch/PKGBUILD's pkgver() already computes for the
      # same commit (same `git describe --long --tags` transform,
      # dots-only so it's valid in both an rpm Version and a pacman
      # pkgver - neither format allows a literal hyphen).
      - name: Determine RPM package version (release builds only)
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          set -euxo pipefail
          RPM_PKGVERSION="$(git describe --long --tags | sed 's/^v//; s/-/.r/; s/-/./')"
          echo "RPM_PKGVERSION=$RPM_PKGVERSION" >> "$GITHUB_ENV"

      - name: rpmbuild
        run: |
          set -euxo pipefail
          defines=(--define "gitcommit $GITCOMMIT")
          if [ -n "${RPM_PKGVERSION:-}" ]; then
            defines+=(--define "pkgversion $RPM_PKGVERSION")
          fi
          rpmbuild -bb "${defines[@]}" \
            --define "_sourcedir $GITHUB_WORKSPACE/rpmbuild-sources" \
            --define "_rpmdir $GITHUB_WORKSPACE/rpmbuild-out" \
            packaging/fedora/hyprav.spec

      - name: Upload .rpm packages
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          name: rpm-packages
          path: rpmbuild-out/**/*.rpm
          if-no-files-found: error
